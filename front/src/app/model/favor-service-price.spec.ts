import { FavorServicePrice } from './favor-service-price';

describe('FavorServicePrice', () => {
  it('should create an instance', () => {
    expect(new FavorServicePrice()).toBeTruthy();
  });
});
