import { CottageReservation } from './cottage-reservation';

describe('CottageReservation', () => {
  it('should create an instance', () => {
    expect(new CottageReservation()).toBeTruthy();
  });
});
